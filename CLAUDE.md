@AGENTS.md

## Claude Code

- Treat `AGENTS.md` as the project source of truth.
- Keep this file as a thin compatibility layer; add Claude-specific rules here only when they cannot live in `AGENTS.md`.
